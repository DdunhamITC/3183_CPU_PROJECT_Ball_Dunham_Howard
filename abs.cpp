#include "abs.h"
#include <iostream>
#include <vector>
using namespace std;

vector <int> absV (vector <int> a)
    {
        if (a[0]==1)
            a[0]=0;
        else
            a[0]=0;
    
        return a;
    }