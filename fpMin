vector <int> fpMin (vector <int > flra, vector <int > flrb)
    {   
        vector <int > flr;
        
        flrb[0]=1;
        flr=addCOMP(flra,flrb);
        if (flr[0]==1)
            return flra;
        else
        {
            return flrb;
        }
        
        
    }
